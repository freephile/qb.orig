- name: Create a new wiki - DB tasks

  hosts: "{{ DB_HOST | default(do_name) }}"

  roles:
    - create_new_wiki_on_db_host

- name: Create a new wiki - MediaWiki tasks

  hosts: "{{ do_name }}"

  roles:
    - create_new_wiki_on_mw_host
